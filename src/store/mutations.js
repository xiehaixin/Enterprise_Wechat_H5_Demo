

const mutations = {
  setStyleColor(state, styleColor) {
    state.styleColor = styleColor;
  },
  setStyleId(state, styleId) {
    state.styleId = styleId;
  },
  setPageStyle(state, pageStyle) {
    state.pageStyle = pageStyle;
  },
}

export default mutations
